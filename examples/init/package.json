{
  "name": "template-name",
  "version": "0.0.1",
  "description": "TemplateName generated using: npx vanil init",
  "scripts": {
    "dev": "vanil dev",
    "dev:head": "npm --prefix ../../ run start:dev -- dev --project-root ./examples/init",
    "build": "vanil build",
    "build:head": "npm --prefix ../../ run start:dev -- build --project-root ./examples/init",
    "preview": "vanil preview",
    "config": "vanil config",
    "config:head": "npm --prefix ../../ run start:dev -- config --project-root ./examples/init",
    "clean": "vanil clean",
    "clean:head": "npm --prefix ../../ run start:dev -- clean --project-root ./examples/init"
  },
  "author": "Aron Homberg <info@aron-homberg>",
  "license": "MIT",
  "browserslist": [
    "> 0.05%",
    "not ie > 0"
  ],
  "vanil": {
    "devOptions": {
      "useOptimizer": false
    }
  },
  "dependencies": {
    "vanil": "^0.0.17"
  }
}