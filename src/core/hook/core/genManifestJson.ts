import { Context } from '../../../@types/context'
import { HookFn } from '../hook'

export const genManifestJson: HookFn = async (context: Context, props: any) => {
  //console.log('genManifestJson', context, props)
  // TODO:
  // generate based on project config
}
